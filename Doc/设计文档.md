# 海外藏中国文物知识管理与服务平台 - 掌上博物馆子系统 设计报告


## 1. 引言

### 1.1 文档目的

本文档用于描述掌上博物馆子系统的总体架构、模块划分、接口设计、数据库设计、UI设计及非功能性设计，为系统开发、测试和后续集成提供技术依据。

### 1.2 子系统概述

掌上博物馆是“海外藏中国文物知识管理与服务平台”的移动端子系统，基于华为 HarmonyOS 开发，采用 ArkTS 语言与 ArkUI 框架。本子系统从知识图谱数据中选取**瓷器**作为展示主题（暂定，待最终确认），为用户提供沉浸式的文物浏览与互动体验。

主要功能包括：
- **文物浏览**：首页展示（卡片/瀑布流）、文物详情页、简单搜索、音视频播放
- **以图搜图**：相册上传图片搜索、拍照搜索、相似度结果展示
- **语音导览**：文物语音讲解播放、语音输入搜索、语音问答交互
- **用户交互**：点赞、收藏及分组管理、评论与回复、用户上传照片
- **用户个人信息管理**：注册登录、个人主页、隐私设置

### 1.3 目标读者

- 前端开发人员（本组全体成员）
- 后端开发人员（知识服务子系统、知识问答子系统、后台管理子系统成员）
- 测试工程师
- 项目管理人员
- 助教及评审教师

### 1.4 术语与缩略语

| 术语 | 全称 | 描述 |
|---|---|---|
| ArkTS | Ark TypeScript | HarmonyOS 官方开发语言，基于 TypeScript 扩展 |
| ArkUI | Ark User Interface | HarmonyOS 声明式 UI 框架 |
| MVVM | Model-View-ViewModel | 前端架构模式，分离 UI、业务逻辑与数据 |
| JWT | JSON Web Token | 用户认证令牌，无状态身份验证 |
| RESTful | Representational State Transfer | API 设计风格 |
| SSE | Server-Sent Events | 服务器推送事件，用于流式响应 |
| CLIP | Contrastive Language-Image Pre-training | 图像特征提取模型 |
| FAISS | Facebook AI Similarity Search | 向量相似度检索引擎 |

### 1.5 参考文献

1. 课程设计题目 - 海外藏中国文物知识管理与服务平台.docx
2. 华为 HarmonyOS 开发者文档：https://developer.harmonyos.com/


## 2. 系统架构设计

### 2.1 架构风格

本子系统采用 **MVVM（Model-View-ViewModel）** 分层架构，主要分为以下三层：

- **View 层（UI 层）**：使用 ArkUI 声明式组件构建页面，负责界面渲染与用户交互。
- **ViewModel 层（业务逻辑层）**：管理页面状态，处理业务逻辑，调用 Model 层接口。
- **Model 层（数据层）**：定义数据结构，封装网络请求与本地存储操作。

此外，本子系统**不直接操作知识图谱**，所有业务数据通过后端 API 获取；以图搜图的特征提取与相似度检索由后端（CLIP + FAISS）完成，移动端仅负责图片采集与结果展示。

### 2.2 总体分层架构图

```mermaid
flowchart TB
    User[用户] --> App[掌上博物馆 App<br/>HarmonyOS / ArkTS + ArkUI]

    App --> View[View 层<br/>ArkUI 声明式页面组件]
    
    View --> VM[ViewModel 层<br/>页面状态管理 / 业务逻辑]
    
    VM --> Model[Model 层<br/>数据模型 / 网络层 / 本地存储]

    Model --> LocalStore[本地存储<br/>Preferences / RDB]
    Model --> HttpUtil[HttpUtil 网络封装<br/>@ohos.net.http]

    HttpUtil --> KnowledgeAPI[知识服务子系统<br/>文物列表/详情/搜索 API]
    HttpUtil --> QA_API[知识问答子系统<br/>问答接口 / SSE 流式]
    HttpUtil --> AdminAPI[后台管理子系统<br/>审核状态 / 内容提交]
    HttpUtil --> ImageAPI[图像检索服务<br/>CLIP + FAISS]
```

### 2.3 技术栈

| 类别 | 技术选型 | 说明 |
|---|---|---|
| 开发平台 | HarmonyOS | 鸿蒙操作系统，课程指定 |
| 开发语言 | ArkTS | 鸿蒙推荐编程语言，基于 TypeScript 扩展 |
| UI 框架 | ArkUI | 声明式 UI 开发框架 |
| 开发工具 | DevEco Studio 6.1.0 (Release) | 统一开发环境 |
| 路由管理 | @ohos.router | 系统级路由能力 |
| 网络请求 | @ohos.net.http | HTTP 数据请求，封装为 HttpUtil |
| 图片加载 | Image 组件 + 图片缓存 | 网络图片加载与本地缓存 |
| 语音识别 | @ohos.speechRecognizer | 语音输入转文字 |
| 语音合成 | @ohos.textToSpeech | 文字转语音播报 |
| 相机调用 | @ohos.multimedia.camera | 拍照搜图功能 |
| 音视频播放 | AVPlayer / Video 组件 | 文物讲解音视频播放 |
| 本地存储 | Preferences / 关系型数据库 | 用户偏好、历史记录、收藏数据缓存 |
| 版本控制 | Git + GitHub | 协作开发 |
| 后端接口 | RESTful API | 与 Web 服务子系统、问答子系统通信 |

### 2.4 模块划分

| 模块编号 | 模块名称 | 负责人 | 功能描述 |
|---|---|---|---|
| M1 | 框架统筹与用户系统 | 潘晨晨 | App 初始化、路由配置、网络层封装；用户注册/登录、个人主页、隐私设置 |
| M2 | 文物浏览 | 郝婧 | 首页卡片/瀑布流展示、文物详情页、简单搜索、音视频播放 |
| M3 | 以图搜图 | 王珍 | 相册选择图片搜索、拍照搜索、相似度结果展示 |
| M4 | 语音导览 | 范力烨 | 文物语音讲解播放、语音输入搜索、语音问答交互 |
| M5 | 用户交互（社交） | 刘清 | 点赞、收藏及分组管理、评论与回复、用户上传照片 |


## 3. 总体设计与公共部分

### 3.1 页面路由设计

#### 3.1.1 路由表

| 页面名称 | 路由路径 | 所属模块 | 说明 |
|---|---|---|---|
| 启动页 | pages/SplashPage | M1 框架 | App 启动加载页，检查登录状态 |
| 登录页 | pages/LoginPage | M1 用户系统 | 用户登录入口 |
| 注册页 | pages/RegisterPage | M1 用户系统 | 新用户注册 |
| 首页 | pages/HomePage | M2 文物浏览 | 文物列表展示（默认进入页） |
| 文物详情页 | pages/ArtifactDetailPage | M2 文物浏览 | 文物详细信息与关联展示 |
| 搜索页 | pages/SearchPage | M2 文物浏览 | 关键字全文搜索 |
| 视频播放页 | pages/VideoPlayPage | M2 文物浏览 | 全屏视频播放 |
| 以图搜图页 | pages/ImageSearchPage | M3 以图搜图 | 图片上传与拍照入口 |
| 相似结果页 | pages/SimilarResultPage | M3 以图搜图 | 相似文物结果列表 |
| 语音导览页 | pages/VoiceGuidePage | M4 语音导览 | 语音讲解播放控制 |
| 语音问答页 | pages/VoiceQAPage | M4 语音导览 | 语音问答对话界面 |
| 个人主页 | pages/ProfilePage | M1 用户系统 | 用户信息、收藏、动态入口 |
| 隐私设置页 | pages/PrivacySettingPage | M1 用户系统 | 隐私选项设置 |
| 收藏列表页 | pages/FavoriteListPage | M5 用户交互 | 收藏夹分组管理 |
| 评论页 | pages/CommentPage | M5 用户交互 | 评论查看、发表与回复 |
| 照片上传页 | pages/PhotoUploadPage | M5 用户交互 | 用户照片上传与说明填写 |

#### 3.1.2 页面跳转流程图

```mermaid
flowchart TD
    Start[启动 App] --> Splash[SplashPage 启动页]
    Splash --> CheckLogin{检查登录状态}
    
    CheckLogin -- 未登录 --> Login[LoginPage 登录页]
    Login --> Register[RegisterPage 注册页]
    Register --> Login
    Login --> Home
    
    CheckLogin -- 已登录 --> Home[HomePage 首页]
    
    Home --> Detail[ArtifactDetailPage 文物详情页]
    Home --> Search[SearchPage 搜索页]
    Home --> ImageSearch[ImageSearchPage 以图搜图]
    Home --> Voice[VoiceGuidePage 语音导览]
    Home --> Profile[ProfilePage 个人主页]
    
    Detail --> Video[VideoPlayPage 视频播放]
    Detail --> Comment[CommentPage 评论页]
    Detail --> Voice
    
    Search --> Detail
    
    ImageSearch --> Similar[SimilarResultPage 相似结果]
    Similar --> Detail
    
    Voice --> VoiceQA[VoiceQAPage 语音问答]
    
    Profile --> Favorite[FavoriteListPage 收藏列表]
    Profile --> Privacy[PrivacySettingPage 隐私设置]
    Profile --> Photo[PhotoUploadPage 照片上传]
    
    Favorite --> Detail
    Photo --> Profile
```

### 3.2 网络层封装设计

本子系统创建统一的 HTTP 工具类 `HttpUtil`，封装 `@ohos.net.http` 能力，全组统一调用。

```typescript
// HttpUtil.ets - 统一网络请求封装
import http from '@ohos.net.http';
import { Preferences } from '@kit.ArkData';

export class HttpUtil {
   private static BASE_URL: string = "https://your-api-server.com/api/v1";

   // 获取存储的 Token
   private static async getToken(): Promise<string> {
      // 从 Preferences 中读取 JWT Token
      return "";
   }

   // GET 请求
   static async get(endpoint: string, params?: Record<string, string>): Promise<ApiResponse> {
      const token = await this.getToken();
      // 构建 URL、添加请求头 Authorization: Bearer {token}
      // 统一处理超时（10秒）、异常捕获
      return { code: 0, message: "success", data: null };
   }

   // POST 请求
   static async post(endpoint: string, data: object): Promise<ApiResponse> {
      const token = await this.getToken();
      // 统一处理
      return { code: 0, message: "success", data: null };
   }

   // 文件上传
   static async upload(endpoint: string, filePath: string): Promise<ApiResponse> {
      // 图片等文件上传封装
      return { code: 0, message: "success", data: null };
   }
}

// 统一接口响应格式
export interface ApiResponse {
code: number;
message: string;
data: object | null;
}
```

### 3.3 公共数据模型定义

以下数据模型全组统一使用，由组长提供。

```typescript
// Artifact.ets - 文物数据模型
export class Artifact {
   objectId: string;        // 文物唯一标识
   title: string;           // 名称
   period: string;          // 年代
   type: string;            // 类型
   material: string;        // 材质
   description: string;     // 描述
   dimensions: string;      // 尺寸
   museum: string;          // 所属博物馆
   location: string;        // 博物馆所在地
   imageUrl: string;        // 图片地址
   detailUrl: string;       // 详情页URL
   accessionNumber: string; // 藏品编号
}

// User.ets - 用户数据模型
export class User {
   userId: number;
   username: string;
   email: string;
   avatar: string;
   status: number;
   privacySetting: PrivacyConfig;
}

// PrivacyConfig.ets - 隐私配置
export class PrivacyConfig {
   showFavorites: boolean;
   showComments: boolean;
   showPhotos: boolean;
}

// FavoriteItem.ets - 收藏项
export class FavoriteItem {
   favoriteId: number;
   objectId: string;
   title: string;
   imageUrl: string;
   groupName: string;
   createdAt: string;
}

// Comment.ets - 评论数据模型
export class Comment {
   commentId: number;
   userId: number;
   username: string;
   avatar: string;
   objectId: string;
   content: string;
   parentId: number | null;
   auditStatus: number;     // 0:待审 1:通过 2:拒绝
   createdAt: string;
}
```


## 4. 接口设计

本子系统不直接操作知识图谱，所有数据通过后端 API 获取。以下为与各子系统的接口约定。

### 4.1 与海外文物知识服务子系统的接口

| 接口名称 | 请求方式 | 路径 | 说明 | 调用模块 |
|---|---|---|---|---|
| 获取文物列表 | GET | /artifacts | 分页获取文物数据，支持排序参数 | M2 文物浏览 |
| 获取文物详情 | GET | /artifacts/{id} | 获取单件文物完整信息含关联实体 | M2 文物浏览 |
| 文物搜索 | GET | /artifacts/search | 关键字全文检索，支持分页 | M2 文物浏览 |
| 获取博物馆列表 | GET | /museums | 获取博物馆基本信息 | M2 文物浏览 |
| 获取相关文物推荐 | GET | /artifacts/{id}/related | 获取与指定文物相似的推荐 | M2 文物浏览 |
| 图像特征检索 | POST | /search/image | 上传图片，返回相似文物列表 | M3 以图搜图 |

### 4.2 与知识问答子系统的接口

| 接口名称 | 请求方式 | 路径 | 说明 | 调用模块 |
|---|---|---|---|---|
| 问答对话 | POST | /qa/chat | 发送文字问题，获取回答（SSE流式） | M4 语音导览 |
| 获取历史列表 | GET | /qa/getHistoryList | 获取用户历史问答记录 | M4 语音导览 |

### 4.3 与后台管理子系统的接口

| 接口名称 | 请求方式 | 路径 | 说明 | 调用模块 |
|---|---|---|---|---|
| 提交评论 | POST | /comments | 提交评论（进入待审核队列） | M5 用户交互 |
| 获取评论列表 | GET | /comments | 获取指定文物的已审核评论 | M5 用户交互 |
| 上传照片 | POST | /photos/upload | 上传用户照片（进入待审核） | M5 用户交互 |
| 获取审核状态 | GET | /audit/status/{contentId} | 查询用户提交内容的审核结果 | M5 用户交互 |
| 点赞/收藏操作 | POST | /user/action | 提交点赞、收藏等行为 | M5 用户交互 |

> **注**：详细的请求参数与响应格式由各模块负责人在 `6.模块详细设计` 中具体定义，并需与其他子系统组长协商确认。


## 5. 数据库设计

### 5.1 本地存储设计

| 存储方式 | 用途 | 关键数据 |
|---|---|---|
| Preferences | 用户偏好与轻量缓存 | JWT Token、首页展示偏好、语音播报速度、隐私设置 |
| 关系型数据库 (RDB) | 本地数据缓存 | 缓存的文物列表、浏览历史、离线收藏数据 |

### 5.2 用户数据库表设计（与后端共用，需协商一致）

以下为建议设计，最终需与知识服务子系统、后台管理子系统协商统一。

```sql
-- 用户表
CREATE TABLE user (
  user_id      INT PRIMARY KEY AUTO_INCREMENT,
  username     VARCHAR(50) NOT NULL UNIQUE,
  password     VARCHAR(255) NOT NULL,        -- bcrypt 加密存储
  email        VARCHAR(100),
  phone        VARCHAR(20),
  avatar       VARCHAR(255),                 -- 头像URL
  status       TINYINT DEFAULT 1,            -- 1:正常 0:禁用
  privacy_setting JSON,                      -- 隐私设置（JSON格式）
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at   DATETIME ON UPDATE CURRENT_TIMESTAMP
);

-- 收藏表
CREATE TABLE favorite (
  favorite_id  INT PRIMARY KEY AUTO_INCREMENT,
  user_id      INT NOT NULL,
  object_id    VARCHAR(50) NOT NULL,
  group_name   VARCHAR(50) DEFAULT '默认收藏夹',
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);

-- 评论表
CREATE TABLE comment (
  comment_id   INT PRIMARY KEY AUTO_INCREMENT,
  user_id      INT NOT NULL,
  object_id    VARCHAR(50) NOT NULL,
  content      TEXT NOT NULL,
  parent_id    INT DEFAULT NULL,             -- 父评论ID，用于回复
  audit_status TINYINT DEFAULT 0,            -- 0:待审 1:通过 2:拒绝
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES user(user_id),
  FOREIGN KEY (parent_id) REFERENCES comment(comment_id)
);

-- 用户上传照片表
CREATE TABLE user_photo (
  photo_id     INT PRIMARY KEY AUTO_INCREMENT,
  user_id      INT NOT NULL,
  object_id    VARCHAR(50),                  -- 关联的文物ID（可选）
  photo_url    VARCHAR(255) NOT NULL,
  description  VARCHAR(500),
  location     VARCHAR(200),                 -- 拍摄地点
  audit_status TINYINT DEFAULT 0,            -- 0:待审 1:通过 2:拒绝
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);

-- 点赞表
CREATE TABLE user_like (
  like_id      INT PRIMARY KEY AUTO_INCREMENT,
  user_id      INT NOT NULL,
  object_id    VARCHAR(50) NOT NULL,
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY (user_id, object_id),
  FOREIGN KEY (user_id) REFERENCES user(user_id)
);
```


## 6. 模块详细设计

> **说明**：本节由各模块负责人分别编写，每人负责自己模块的详细设计内容。

### 6.1 文物浏览模块（郝婧 编写）

#### 6.1.1 模块概述
文物浏览模块（M2）是掌上博物馆子系统的核心展示模块。主要职责包括：
- **首页轮播展示**：在 `ShowPage` 中随机展示6件精选文物，自动轮播，点击跳转详情页。
- **文物列表与筛选排序**：在 `HomePage` 中以网格/列表双视图展示全部文物，支持全文关键字搜索，支持按年代、类型、材质、博物馆筛选，按热门/名称/年代排序，支持分页加载。
- **文物详情页**：在 `ArtifactDetailPage` 中展示文物高清图片多图轮播（支持双指缩放）、完整元数据、同年代关联文物、相关推荐文物。将用户的点赞、收藏、评论操作通过事件传递给 M5（用户交互模块），语音讲解跳转至 M4（语音导览模块）

当前原型阶段，所有文物数据来源于本地 `mockArtifacts`，筛选、排序、分页均在前端完成；推荐关联逻辑基于年代/类型匹配前端实现。后续将逐步迁移至后端服务接口。
#### 6.1.2 UI 页面设计
文物浏览模块共包含 3 个页面，各页面组件树如下。

**展示页组件树**
```mermaid
flowchart TD
    A[ShowPage] --> B[品牌标题区]
    A --> C[轮播卡片容器]
    A --> D[古典分隔线]
    A --> E[文化标语]
    A --> F[精选馆藏横排]

    B --> B1[掌上博物馆]
    B --> B2[触摸历史·感受匠心]

    C --> C1[Swiper轮播组件]
    C1 --> C2[文物卡片]
    C2 --> C3[图片 Image]
    C2 --> C4[文物名称]
    C2 --> C5[年代]

    F --> F1[精选馆藏标题]
    F --> F2[横向Row]
    F2 --> F3[文物缩略图列]
    F3 --> F4[图片 Image]
    F3 --> F5[名称]
    F3 --> F6[年代]
```

**首页组件树**

```mermaid
flowchart TD
    A[HomePage] --> B[导航栏]
    A --> C[搜索框]
    A --> D[折叠式筛选栏]
    A --> E[排序按钮行]
    A --> F[内容区域]

    B --> B1[博物馆图标]
    B --> B2[掌上博物馆]
    B --> B3[视图切换按钮]

    D --> D1[年代筛选行（常显）]
    D --> D2[展开/收起按钮]
    D --> D3[展开区：类型/材质/博物馆筛选行]

    E --> E1[排序：热门/名称/年代]

    F --> F1{视图模式}
    F1 -- 网格模式 --> F2[List 双列网格]
    F1 -- 列表模式 --> F3[List 单列列表]
    F2 --> F4[CardView]
    F3 --> F5[ListView]
    F4/F5 --> F6[图片+文字卡片]
    F4/F5 --> F7[点击跳转详情页]
```

**文物详情页组件树**

```mermaid
flowchart TD
    A[ArtifactDetailPage] --> B[图片轮播区]
    A --> C[基本信息区]
    A --> D[同年代文物标签区]
    A --> E[相关推荐横滑区]
    A --> F[语音交互区]
    A --> G[点赞/收藏区]
    A --> H[评论区]
    A --> I[照片上传区]

    B --> B1[Swiper图片轮播]
    B --> B2[双指缩放/平移手势]
    B --> B3[页码指示器]

    C --> C1[文物名称]
    C --> C2[年代/类型/材质标签]
    C --> C3[收藏机构]
    C --> C4[文物介绍文本]

    F --> F1[语音讲解按钮]
    F --> F2[语音问答按钮]
    F --> F3["倍速控制(0.75/1/1.25/1.5)"]
    F --> F4[上一段/下一段]
    F --> F5[停止/重播]

    G --> G1["点赞按钮(状态+计数)"]
    G --> G2["收藏按钮(状态+计数)"]
    G --> G3[收藏分组选择器]

    H --> H1["评论列表(支持回复)"]
    H --> H2[评论输入框]
    H --> H3[发表按钮]

    I --> I1[拍摄地点输入]
    I --> I2[简短说明输入]
    I --> I3[选择照片并上传按钮]
```

#### 6.1.3 组件交互流程

**展示页轮播加载与跳转**
```mermaid
sequenceDiagram
    participant User as 用户
    participant ShowPage as ShowPage
    participant MockData as mockArtifacts

    User->>ShowPage: 打开ShowPage
    ShowPage->>MockData: 随机取6件文物
    MockData-->>ShowPage: showcaseList
    ShowPage->>ShowPage: 自动轮播(间隔3秒)
    ShowPage-->>User: 渲染轮播卡片

    User->>ShowPage: 点击轮播卡片
    ShowPage->>ShowPage: router.pushUrl 带objectId
    ShowPage-->>User: 跳转ArtifactDetailPage

    User->>ShowPage: 点击精选馆藏卡片
    ShowPage-->>User: 同样跳转详情页
```

**首页初始化与筛选排序**

```mermaid
sequenceDiagram
    participant User as 用户
    participant HomePage as HomePage
    participant MockData as mockArtifacts

    User->>HomePage: 打开HomePage
    HomePage->>MockData: 加载全部文物
    MockData-->>HomePage: allArtifacts
    HomePage->>HomePage: 提取筛选选项(period/type/material/museum)
    HomePage->>HomePage: 初次筛选+排序(默认热门)
    HomePage->>HomePage: 分页截取(每页10条)
    HomePage-->>User: 渲染网格/列表视图

    User->>HomePage: 点击排序按钮(热门/名称/年代)
    HomePage->>HomePage: 按规则重新排序
    HomePage->>HomePage: 重置分页并刷新列表
    HomePage-->>User: 更新列表

    User->>HomePage: 展开筛选栏，选择类型/材质/博物馆
    HomePage->>HomePage: 应用新筛选条件，重置分页
    HomePage-->>User: 刷新列表

    User->>HomePage: 上滑到底
    HomePage->>HomePage: 加载更多(currentPage++)
    alt 还有更多数据
        HomePage-->>User: 追加新卡片
    else 已无更多
        HomePage-->>User: 显示"已经到底了"
    end
```

**搜索交互流程**
```mermaid
sequenceDiagram
    participant User as 用户
    participant HomePage as HomePage
    participant Timer as 防抖定时器

    User->>HomePage: 在搜索框输入关键字
    HomePage->>Timer: 清除之前定时器
    HomePage->>Timer: 启动新定时器(300ms)
    Timer-->>HomePage: 超时触发
    HomePage->>HomePage: 根据keyword筛选文物(字段:title/museum/period/type/material)
    HomePage->>HomePage: 保留当前排序和筛选条件
    alt 有匹配结果
        HomePage-->>User: 更新列表
    else 无结果
        HomePage-->>User: 显示空状态
    end

    User->>HomePage: 点击清空按钮(✕)
    HomePage->>HomePage: keyword置空，重新加载全部
    HomePage-->>User: 恢复全部列表
```

**文物详情页加载与核心交互**
```mermaid
sequenceDiagram
    participant User as 用户
    participant DetailPage as ArtifactDetailPage
    participant MockData as mockArtifacts
    participant InteractionStore as InteractionStore(本地)
    participant VoiceService as MuseumVoiceService

    User->>DetailPage: 从列表/轮播图/相关推荐进入(带objectId)
    DetailPage->>MockData: 根据id查找文物
    MockData-->>DetailPage: artifact对象
    DetailPage->>DetailPage: 初始化图片数组、相关推荐(同年代/同类型)
    DetailPage->>InteractionStore: 获取点赞/收藏/评论状态
    InteractionStore-->>DetailPage: 返回当前用户互动数据
    DetailPage-->>User: 完整渲染详情页

    User->>DetailPage: 左右滑动图片
    DetailPage->>DetailPage: 更新currentImageIndex

    User->>DetailPage: 双指缩放图片
    DetailPage->>DetailPage: 修改scaleValue/offsetX/offsetY

    User->>DetailPage: 点击"同年代文物"标签 或 "相关推荐"卡片
    DetailPage->>DetailPage: router.replaceUrl(直接替换当前页)
    DetailPage-->>User: 加载新文物详情

    User->>DetailPage: 点击"语音讲解"
    DetailPage->>VoiceService: buildArtifactGuide + speak
    VoiceService-->>User: 语音播放

    User->>DetailPage: 点击倍速/上一段/下一段/停止/重播
    DetailPage->>VoiceService: 调用对应控制方法
    VoiceService-->>User: 调整播放

    User->>DetailPage: 点击点赞/收藏
    alt 已登录
        DetailPage->>InteractionStore: toggleLike / toggleFavorite
        InteractionStore-->>DetailPage: 返回新状态(active/count)
        DetailPage-->>User: 更新按钮样式和计数
    else 未登录
        DetailPage-->>User: 弹窗提示请先登录，跳转登录页
    end

    User->>DetailPage: 发表评论/回复评论
    DetailPage->>InteractionStore: addComment(含审核状态)
    InteractionStore-->>DetailPage: 保存成功
    DetailPage->>DetailPage: 刷新评论区，定时1.6秒后重新拉取(模拟审核)
    DetailPage-->>User: 显示待审核评论

    User->>DetailPage: 选择照片上传
    DetailPage->>DetailPage: pickSingleImageUri调用相册
    DetailPage->>InteractionStore: addUpload(本地存储)
    DetailPage-->>User: 提示"照片已上传，等待审核"
```
**视图切换与分页加载补充流程**
```mermaid
sequenceDiagram
    participant User as 用户
    participant HomePage as HomePage

    User->>HomePage: 点击视图切换图标(📱/📋)
    HomePage->>HomePage: 切换viewMode(0:网格,1:列表)
    HomePage-->>User: 用新布局重新渲染当前displayList

    User->>HomePage: 在列表末尾点击"加载更多"按钮
    HomePage->>HomePage: currentPage++
    HomePage->>HomePage: 重新计算displayList(前N页)
    alt 还有数据
        HomePage-->>User: 追加卡片，按钮保留
    else 已无数据
        HomePage->>HomePage: hasMore=false，按钮变为"已经到底了"
        HomePage-->>User: 显示文本提示
    end
```

#### 6.1.4 接口调用设计

本模块所有文物数据当前来源于本地 `mockArtifacts`，未实际调用后端 API。但为后续与知识服务子系统集成，已按设计文档中的接口约定预留调用位置。以下为完整的接口定义及当前实现状态说明。

**接口列表**

| 序号 | 接口用途 | 请求方式 | 路径 | 调用页面 | 当前实现状态 |
|------|----------|----------|------|----------|-------------|
| 1 | 获取文物列表 | GET | `/artifacts` | HomePage | **未使用**，使用 mock 数据 |
| 2 | 获取文物详情 | GET | `/artifacts/{objectId}` | ArtifactDetailPage | **未使用**，使用 mock 数据 |
| 3 | 文物搜索 | GET | `/artifacts/search` | HomePage（搜索框） | **未使用**，前端本地筛选 |
| 4 | 获取相关文物推荐 | GET | `/artifacts/{objectId}/related` | ArtifactDetailPage | **未使用**，前端基于年代/类型匹配 |

> **说明**：当前原型阶段，所有数据操作均在前端完成。后续接入后端时，仅需将 `HomePage` 和 `ArtifactDetailPage` 中的数据获取逻辑从 `mockArtifacts` 替换为 `HttpUtil` 调用，数据模型保持不变即可。

---


**接口 1：获取文物列表**

| 项目 | 内容 |
|------|------|
| 请求方式 | GET |
| 接口路径 | `/artifacts` |
| 调用场景 | 首页首次加载、下拉刷新、上拉分页加载、切换排序方式 |

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | int | 否 | 页码，默认 1 |
| size | int | 否 | 每页条数，默认 20 |
| sort | string | 否 | 排序方式：`hot`（热门，默认）、`name`（名称）、`period`（年代） |
| period | string | 否 | 年代筛选，如“唐”、“宋” |
| type | string | 否 | 类型筛选，如“瓷器”、“青铜器” |
| material | string | 否 | 材质筛选，如“青花瓷”、“青铜” |
| museum | string | 否 | 博物馆筛选 |
| keyword | string | 否 | 搜索关键字（用于全局搜索） |

响应参数：

```json
{
   "code": 200,
   "message": "success",
   "data": {
      "total": 150,
      "page": 1,
      "size": 20,
      "items": [
         {
            "objectId": "artifact_001",
            "title": "青花云龙纹象耳瓶",
            "period": "元",
            "type": "瓷器",
            "material": "青花瓷",
            "museum": "大英博物馆",
            "imageUrl": "https://xxx/thumbnail/artifact_001.jpg",
            "popularity": 98
         }
      ]
   }
}
```

响应字段说明：

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 状态码，200 表示成功 |
| message | string | 提示信息 |
| data.total | int | 文物总数 |
| data.page | int | 当前页码 |
| data.size | int | 每页条数 |
| data.items | array | 文物数组 |
| data.items[].objectId | string | 文物唯一标识 |
| data.items[].title | string | 文物名称 |
| data.items[].period | string | 年代 |
| data.items[].type | string | 类型 |
| data.items[].material | string | 材质 |
| data.items[].museum | string | 所属博物馆 |
| data.items[].imageUrl | string | 缩略图地址 |
| data.items[].popularity | int | 热度值（用于排序） |

---

**接口 2：获取文物详情**

| 项目 | 内容                        |
|------|---------------------------|
| 请求方式 | GET                       |
| 接口路径 | `/artifacts/{objectId}`   |
| 调用场景 | 从首页列表、搜索结果、相关推荐、轮播图点击某文物时 |

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| objectId | string | 是 | 文物唯一标识，拼接在 URL 路径中 |

响应示例：

```json
{
   "code": 200,
   "message": "success",
   "data": {
      "objectId": "artifact_001",
      "title": "青花云龙纹象耳瓶",
      "period": "元",
      "type": "瓷器",
      "material": "青花瓷",
      "description": "此瓶盘口，长颈，瘦腹，台足...",
      "dimensions": "高 63.6cm，口径 14.5cm",
      "museum": "大英博物馆",
      "location": "英国伦敦",
      "accessionNumber": "PDF.100",
      "imageUrls": [
         "https://xxx/hd/artifact_001_01.jpg",
         "https://xxx/hd/artifact_001_02.jpg"
      ],
      "videoUrl": "https://xxx/video/artifact_001.mp4"
   }
}
```

响应字段说明：

| 字段 | 类型 | 说明 |
|------|------|------|
| data.objectId | string | 文物唯一标识 |
| data.title | string | 文物名称 |
| data.period | string | 年代 |
| data.type | string | 类型 |
| data.material | string | 材质 |
| data.description | string | 详细描述 |
| data.dimensions | string | 尺寸 |
| data.museum | string | 所属博物馆 |
| data.location | string | 博物馆所在地 |
| data.accessionNumber | string | 藏品编号 |
| data.imageUrls | array | 高清图片地址数组 |
| data.videoUrl | string | 介绍视频地址（可选） |

---
**接口 3：文物搜索**

| 项目 | 内容 |
|------|------|
| 请求方式 | GET |
| 接口路径 | /artifacts/search |
| 调用场景 | 用户在搜索框输入关键字后自动请求（前端防抖 300ms） |

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| q | string | 是 | 搜索关键字，最短 1 个字符 |
| page | int | 否 | 页码，默认 1 |
| size | int | 否 | 每页条数，默认 20 |

响应示例：

```json
{
   "code": 200,
   "message": "success",
   "data": {
      "total": 8,
      "page": 1,
      "size": 20,
      "items": [
         {
            "objectId": "artifact_023",
            "title": "青花花鸟纹盘",
            "period": "明",
            "type": "瓷器",
            "material": "青花瓷",
            "museum": "大都会艺术博物馆",
            "imageUrl": "https://xxx/thumbnail/artifact_023.jpg"
         }
      ]
   }
}
```
响应字段说明：
同接口 1 的 items 结构

---
**接口 4：获取相关文物推荐**

| 项目 | 内容 |
|------|------|
| 请求方式 | GET |
| 接口路径 | `/artifacts/{objectId}/related` |
| 调用场景 | 进入文物详情页后，加载相关文物推荐区域 |

请求参数：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| objectId | string | 是 | 当前文物唯一标识，拼接在 URL 路径中 |
| count | int | 否 | 推荐数量，默认 6 |

响应示例：

```json
{
   "code": 200,
   "message": "success",
   "data": {
      "items": [
         {
            "objectId": "artifact_045",
            "title": "元青花鬼谷子下山图罐",
            "period": "元",
            "type": "瓷器",
            "museum": "大英博物馆",
            "imageUrl": "https://xxx/thumbnail/artifact_045.jpg"
         }
      ]
   }
}
```

响应字段说明：
同接口 1 的 items 结构。

---
**错误响应格式**

所有接口在异常情况下的统一响应格式：

```json
{
    "code": 400,
    "message": "参数错误：q 不能为空",
    "data": null
}
```

| 状态码  | 含义	          |前端处理|
|------|--------------|----|
| 400	 | 请求参数错误       |提示用户输入不合法|
| 401  | 	Token 无效或过期 | 	跳转登录页          |
| 404	 | 文物不存在	       | 提示“文物未找到”并返回上一页 |
| 500  | 	服务器内部错误	    | 提示“服务器繁忙，请稍后再试” |

#### 6.1.5 数据结构设计

本模块在公共数据模型的基础上，针对首页轮播、文物列表、文物详情等不同场景，定义了以下前端数据模型和状态结构。

**1. 文物（Artifact）数据模型**

该模型用于表示单件文物的完整信息，在 `ShowPage`、`HomePage`、`ArtifactDetailPage` 中统一使用。

```typescript
export class Artifact {
   id: string;               // 文物唯一标识（对应接口 objectId）
   title: string;            // 文物名称
   period: string;           // 年代
   type: string;             // 类型（如瓷器、青铜器）
   material: string;         // 材质（如青花瓷、青铜）
   description: string;      // 详细描述
   dimensions: string;       // 尺寸（可选，当前代码未使用但保留）
   museum: string;           // 所属博物馆
   location: string;         // 博物馆所在地（可选）
   accessionNumber: string;  // 藏品编号（可选）
   imageUrl: string;         // 缩略图地址（列表展示用）
   imageUrls: string[];      // 高清图片地址数组（详情页轮播用）
   videoUrl?: string;        // 介绍视频地址（可选，预留）
   popularity?: number;      // 热度值（用于“热门”排序）
}
```

**2. 首页轮播页（ShowPage）数据模型**

```typescript
// ShowPage 中的状态数据
private showcaseList: Artifact[] = mockArtifacts.sort(() => Math.random() - 0.5).slice(0, 6);  // 随机取6件用于轮播
private featuredList: Artifact[] = mockArtifacts.slice(0, 3);                                   // 前3件用于精选馆藏
@State currentImageIndex: number = 0;                                                           // 当前轮播索引
```

**3. 文物列表页（HomePage）数据模型**

筛选选项枚举与状态

```typescript
// HomePage 中的筛选相关状态
@State periods: string[] = [];      // 年代去重列表（包含“全部”）
@State types: string[] = [];        // 类型去重列表
@State materials: string[] = [];    // 材质去重列表
@State museums: string[] = [];      // 博物馆去重列表

@State filterPeriod: string = '全部';
@State filterType: string = '全部';
@State filterMaterial: string = '全部';
@State filterMuseum: string = '全部';
```
排序方式枚举

```typescript
// 排序类型（代码中使用字符串联合类型）
type SortBy = 'hot' | 'name' | 'period';
@State sortBy: SortBy = 'hot';
```
视图模式枚举

```typescript
@State viewMode: number = 0;   // 0: 网格模式（两列），1: 列表模式（单列）
```
分页与列表状态

```typescript
private allArtifacts: Artifact[] = mockArtifacts;      // 全部文物数据源
@State displayList: Artifact[] = [];                   // 当前展示的文物列表（经过筛选/排序/分页）
@State currentPage: number = 1;                       // 当前页码
private pageSize: number = 10;                         // 每页数量
@State totalFilteredList: Artifact[] = [];             // 筛选排序后的完整列表（未分页）
@State hasMore: boolean = false;                       // 是否还有更多数据
```
搜索状态

```typescript
@State searchKeyword: string = '';                     // 搜索关键字
private searchTimer: number = -1;                      // 防抖定时器ID
```
折叠筛选栏状态

```typescript
@State showMoreFilters: boolean = false;               // 是否展开更多筛选行
```

**4. 文物详情页（ArtifactDetailPage）数据模型**

核心文物数据

```typescript
@State artifact: Artifact | null = null;               // 当前文物对象
@State allImageUrls: string[] = [];                    // 所有图片URL（从 artifact.imageUrls 或 [artifact.imageUrl] 派生）
@State currentImageIndex: number = 0;                  // 当前图片索引
```
图片手势状态

```typescript
@State scaleValue: number = 1;                         // 图片缩放比例（0.5~3）
@State offsetX: number = 0;                            // 双指平移X偏移
@State offsetY: number = 0;                            // 双指平移Y偏移
```
相关推荐数据

```typescript
// 同年代文物（标签云形式）
interface RelatedEntity {
title: string;
id: string;
}
@State relatedEntities: RelatedEntity[] = [];

// 相关推荐文物（横向滚动卡片）
@State relatedList: Artifact[] = [];
```
语音交互状态

```typescript
@State currentVoiceSpeed: number = 1;                  // 当前语音播报倍速（0.75/1/1.25/1.5）
@State voiceProgressText: string = '0 / 0';            // 当前段落进度文本（由 MuseumVoiceService 提供）
```
点赞/收藏/评论状态（由 InteractionStore 管理，此处仅声明与UI绑定的状态）

```typescript
@State likeSummary: InteractionSummary = { active: false, count: 0 };
@State favoriteSummary: InteractionSummary = { active: false, count: 0 };
@State favoriteGroups: string[] = [];                  // 用户收藏分组列表
@State selectedFavoriteGroup: string = '';             // 当前选中的分组
@State comments: CommentItem[] = [];                   // 评论列表
```
评论输入状态

```typescript
@State commentInput: string = '';                      // 评论输入框内容
@State isSubmittingComment: boolean = false;           // 是否正在提交评论
@State replyTarget: CommentItem | null = null;         // 当前回复的目标评论
```
照片上传状态

```typescript
@State isUploadingPhoto: boolean = false;              // 是否正在上传
@State uploadLocation: string = '';                    // 拍摄地点（默认填充文物博物馆）
@State uploadDescription: string = '';                 // 简短说明
```

**5. 辅助数据模型**

筛选配置模型（用于 FilterBar 组件）

```typescript
interface FilterConfig {
label: string;           // 筛选标签（“年代”、“类型”、“材质”、“博物馆”）
options: string[];       // 可选值列表（包含“全部”）
value: string;           // 当前选中的值
onChange: (val: string) => void;
}
```
朝代排序映射（用于“年代”排序）

```typescript
private readonly periodOrderMap: Map<string, number> = new Map([
['良渚文化', 1], ['新石器时代', 1], ['仰韶文化', 1],
['夏', 2], ['商', 3], ['西周', 4], ['春秋', 5], ['战国', 6],
['秦', 7], ['西汉', 8], ['东汉', 9], ['三国', 10],
['西晋', 11], ['东晋', 12], ['南北朝', 13], ['隋', 14],
['唐', 15], ['五代十国', 16], ['北宋', 17], ['南宋', 18],
['辽', 19], ['金', 20], ['元', 21], ['明', 22], ['清', 23],
['民国', 24]
]);
```

**6. 模型关系与数据流向**

| 模型 | 使用页面/组件 | 数据来源 | 说明 |
|------|--------------|----------|------|
| Artifact | ShowPage, HomePage, ArtifactDetailPage | mockArtifacts（当前）/ 后续接入接口 | 核心文物数据 |
| showcaseList | ShowPage 轮播区 | 从 mockArtifacts 随机取6件 | 仅用于首页轮播 |
| featuredList | ShowPage 精选馆藏区 | mockArtifacts 前3件 | 仅用于首页横排展示 |
| HomePage 各筛选/排序/分页状态 | HomePage | 前端本地状态 | 管理列表展示逻辑 |
| ArtifactDetailPage 图片手势状态 | 详情页图片区 | 前端本地状态 | 仅用于交互反馈 |
| relatedEntities / relatedList | 详情页推荐区 | 基于当前文物 period/type 从 mockArtifacts 筛选 | 前端实时计算 |
| voiceProgressText | 语音控制区 | MuseumVoiceService | 由语音服务提供 |
| 点赞/收藏/评论/上传状态 | 详情页各交互区 | InteractionStore（本地存储） | 当前持久化到 Preferences，后续接后端 |

**7. 本地缓存说明**

当前原型阶段未使用 RDB 或 Preferences 缓存文物列表数据，所有文物数据均从 mockArtifacts 实时读取。后续可参照设计文档中的 ArtifactCache 模型，在 HomePage 中增加以下缓存逻辑：

```typescript
// ArtifactCache.ets（预留）
export class ArtifactCache {
cacheData: ArtifactListItem[];
cacheTime: number;
expireDuration: number = 7200000; // 2小时
isValid(): boolean { return Date.now() - this.cacheTime < this.expireDuration; }
}
```
注意：互动数据（点赞、收藏、评论、上传照片）已通过 InteractionStore 持久化到 Preferences，该部分不属于文物浏览模块的数据管理范围，由用户交互模块（M5）维护。

---

### 6.2 以图搜图模块（王珍 编写）

#### 6.2.1 模块概述
[请在此描述模块职责与功能范围]

#### 6.2.2 图像检索流程设计
[请在此插入时序图，展示：相册选择→上传→特征提取→检索→结果展示全流程]

#### 6.2.3 相机调用设计
[请在此描述调用 @ohos.multimedia.camera 的流程与权限处理]

#### 6.2.4 接口调用设计
[请在此列出图片上传接口与检索结果接口的参数]

#### 6.2.5 相似度结果展示设计
[请在此描述结果列表的排序方式与展示样式]

---

### 6.3 语音导览模块设计（范力烨 编写）

#### 6.3.1 模块概述

语音导览模块由范力烨负责，主要实现文物语音讲解、语音搜索和语音问答功能。该模块通过调用 HarmonyOS 的语音合成和语音识别能力，为用户提供“听文物介绍、说关键词搜索、语音提问”的交互体验。

模块主要功能包括：根据文物名称、年代、类型、材质、馆藏地点和简介生成语音讲解文本；调用语音合成接口播放文物讲解内容；支持播放、停止、重播、上一段、下一段、倍速和进度控制；调用语音识别接口，将用户语音转换为搜索关键词；根据识别结果搜索文物并跳转到详情页；支持围绕当前文物进行简单问答，并将回答内容语音播报。

---

#### 6.3.2 语音播讲设计

语音播讲功能用于在文物详情页播放当前文物的介绍内容。系统首先根据当前文物对象生成讲解文本，然后调用文本转语音接口进行播放。

基本流程如下：

1. 用户进入文物详情页；
2. 点击“语音讲解”按钮；
3. 前端调用 `buildArtifactGuide(artifact)` 生成讲解文本；
4. 调用 `speak(text, speed)` 播放语音；
5. 用户可进行停止、重播、上一段、下一段和倍速调整等操作。

讲解文本主要由文物的 `title`、`period`、`type`、`material`、`museum` 和 `description` 等字段组成。其中 `title` 表示文物名称，`period` 表示文物年代，`type` 表示文物类型，`material` 表示文物材质，`museum` 表示收藏博物馆，`description` 表示文物简介。

播放控制主要包括以下接口：

| 接口 | 功能 |
|---|---|
| `speak(text, speed)` | 播放指定文本 |
| `stop()` | 停止当前播放 |
| `replay()` | 重新播放 |
| `previousParagraph()` | 播放上一段 |
| `nextParagraph()` | 播放下一段 |
| `setSpeed(speed)` | 设置播放倍速 |
| `getProgressText()` | 获取播放进度 |

其中，倍速参数 `speed` 用于控制语音播放速度，进度控制通过将讲解文本按段落切分实现，用户可以查看当前播放段落并切换上一段或下一段。

---

#### 6.3.3 语音搜索设计

语音搜索功能用于将用户的语音输入转换为文本，并根据识别结果搜索文物。

基本流程如下：

1. 用户点击语音搜索按钮；
2. 系统调用语音识别接口开始监听；
3. 用户说出搜索内容，例如“搜索青铜器”；
4. 语音识别模块返回文本结果；
5. 系统对识别文本进行清洗，去除“搜索”“查找”“我想看”等无关词；
6. 调用文物搜索逻辑匹配文物；
7. 若找到文物，则跳转到对应详情页；若未找到，则提示用户没有相关结果。

主要接口如下：

| 接口 | 功能 |
|---|---|
| `startVoiceSearch(onText)` | 开始语音识别 |
| `finishVoiceSearch()` | 结束语音识别 |
| `normalizeSearchText(text)` | 清洗识别文本 |
| `searchArtifactByText(text)` | 根据关键词搜索文物 |
| `handleVoiceSearch(text)` | 处理搜索结果并跳转 |

语音搜索实现了从“语音输入”到“文本识别”，再到“文物搜索与页面跳转”的完整流程，提升了用户检索文物的便利性。

---

#### 6.3.4 语音问答设计

语音问答功能用于支持用户围绕当前文物进行提问。当前实现主要基于文物详情字段进行规则匹配，能够回答文物年代、材质、类型、收藏地点和简介等常见问题。

基本流程如下：

1. 用户在文物详情页提出问题；
2. 系统获取当前文物对象和问题文本；
3. 调用 `answerQuestion(artifact, question)` 生成回答；
4. 调用 `speak(answer, speed)` 播放回答内容。

支持的问题类型如下：

| 问题类型 | 示例问题 | 回答内容 |
|---|---|---|
| 年代 | 这个文物是什么年代的？ | 返回 `period` |
| 材质 | 它是什么材质？ | 返回 `material` |
| 类型 | 它属于什么类型？ | 返回 `type` |
| 馆藏 | 它收藏在哪里？ | 返回 `museum` |
| 简介 | 介绍一下这个文物 | 返回 `description` |

后续可对接知识问答子系统，通过 SSE 流式响应实现更复杂的智能问答。前端发送问题、文物 ID 和会话 ID，后端持续返回回答片段，前端边接收边显示，并可按句子或段落调用语音播报接口进行播放。

---

#### 6.3.5 接口调用设计

语音导览模块主要调用本地语音服务接口、文本转语音接口和语音识别接口。

本地语音服务接口如下：

| 接口 | 参数 | 功能 |
|---|---|---|
| `buildArtifactGuide` | `artifact` | 生成文物讲解文本 |
| `speak` | `text, speed` | 播放语音 |
| `stop` | 无 | 停止播放 |
| `replay` | 无 | 重播 |
| `setSpeed` | `speed` | 设置倍速 |
| `previousParagraph` | 无 | 播放上一段 |
| `nextParagraph` | 无 | 播放下一段 |
| `getProgressText` | 无 | 获取播放进度 |
| `startVoiceSearch` | `onText` | 开始语音搜索 |
| `finishVoiceSearch` | 无 | 结束语音识别 |
| `searchArtifactByText` | `text` | 搜索文物 |
| `answerQuestion` | `artifact, question` | 生成问答结果 |

文本转语音接口主要用于将讲解文本和问答结果转换为语音播放。主要参数包括：

| 参数 | 说明 |
|---|---|
| `language` | 语音语言，设置为中文 |
| `text` | 需要播放的文本 |
| `speed` | 播放速度 |
| `volume` | 音量 |
| `pitch` | 音调 |
| `requestId` | 本次播放任务编号 |

语音识别接口主要用于将用户语音转换为文本。主要参数包括：

| 参数 | 说明 |
|---|---|
| `language` | 识别语言，设置为中文 |
| `sessionId` | 本次识别会话编号 |
| `sampleRate` | 采样率 |
| `recognitionMode` | 识别模式 |
| `maxAudioDuration` | 最大识别时长 |

后端预留接口如下：

| 接口 | 功能 |
|---|---|
| `/api/artifacts/search` | 根据关键词搜索文物 |
| `/api/artifacts/{id}` | 获取文物详情 |
| `/api/qa/stream` | 语音问答流式返回 |
| `/api/qa/history` | 获取问答历史 |

其中 `/api/qa/stream` 可使用 SSE 方式返回问答内容，实现边生成、边显示、边播报。

---

### 6.4 用户交互模块（刘清 编写）

#### 6.4.1 模块概述
用户交互模块负责承接用户在文物详情页和个人中心中的社交化行为，属于前端本地状态与持久化的核心模块。当前阶段采用 `Preferences` 作为本地存储载体，围绕以下四类能力展开：
- **点赞与收藏**：记录用户对文物的偏好状态，并在详情页、个人中心、收藏列表中联动展示。
- **评论与回复**：支持用户针对文物发表评论、查看审核状态，并为后续接入后端审核流预留数据结构。
- **照片上传**：支持从相册选择图片，保存文物关联、时间、地点和说明信息。
- **浏览记录与统计**：在进入详情页时自动记录浏览历史，并在个人中心汇总点赞、收藏、评论、照片数量。

模块遵循“当前先本地原型、后续再接后端接口”的设计原则，数据结构和页面入口均按正式产品形态组织。

#### 6.4.2 点赞收藏流程设计
点赞/收藏的交互流程如下：
1. 用户进入文物详情页，页面加载该文物的点赞/收藏状态与计数。
2. 未登录用户点击点赞或收藏时，系统提示“请先登录”，并跳转登录页。
3. 已登录用户点击点赞按钮时：
   - 若当前未点赞，则写入本地点赞映射，按钮切换为高亮状态，数量 `+1`。
   - 若当前已点赞，则删除本地记录，按钮恢复默认状态，数量 `-1`。
4. 已登录用户点击收藏按钮时：
   - 若当前未收藏，则将文物写入当前用户的收藏列表，默认归入“默认收藏夹”。
   - 若当前已收藏，则将文物从收藏列表中移除。
5. 收藏夹页面按当前用户维度读取收藏数据，并展示时间、分组等信息；分组管理在原型阶段采用轻量方式实现，可通过固定分组集合进行切换。

本模块将点赞与收藏分离存储，避免相互耦合；同时在个人中心汇总统计数量，提升反馈一致性。

模块遵循“当前先本地原型、后续再接后端接口”的设计原则，数据结构和页面入口均按正式产品形态组织。

#### 6.4.3 评论功能设计
评论功能采用“详情页快速输入 + 个人中心统一查看”的双入口设计：
- **详情页评论区**：展示若干条公开评论或本地种子评论，并提供单行输入框和“发表”按钮，方便用户快速参与。
- **我的评论页**：聚合当前用户提交过的评论，展示关联文物、评论内容、提交时间和审核状态。
- **回复能力**：数据模型中保留 `parentId` 与 `replyTo` 字段，为后续完整回复链路预留结构；当前原型阶段可先以“回复某人：内容”的形式展示。

审核状态流转设计：
1. 用户提交评论后，前端先执行非空、长度和敏感词预校验。
2. 通过校验的评论写入本地持久化存储，并标记为 `pending`。
3. 评论在“我的评论”中始终可见，但公开区默认只展示种子评论和已通过内容。
4. 后续接入后端审核接口时，可将本地状态映射为 `pending / approved / rejected` 三态。

#### 6.4.4 照片上传功能设计
照片上传采用“选择图片 -> 保存元数据 -> 标记审核中 -> 个人中心查看”的闭环设计：
1. 用户在详情页或“上传的照片”页面点击上传入口。
2. 系统调用图片选择器，获取单张图片 URI。
3. 前端收集与图片相关的文物编号、文物标题、地点、说明、上传时间等信息。
4. 系统将上传记录写入本地存储，并统一标记为 `pending`。
5. “上传的照片”页面按卡片形式展示缩略图、关联文物、描述、地点和审核状态。

原型阶段不发起真实网络请求，但整个数据结构、权限声明和状态设计都与后续真实接口保持一致。

#### 6.4.5 接口调用设计
当前阶段以前端本地持久化为主，不强制依赖后端接口；但为后续联调预留如下接口约定：

| 功能 | 方法 | 路径 | 关键参数 | 说明 |
|---|---|---|---|---|
| 点赞/收藏行为提交 | POST | `/user/action` | `userId`, `objectId`, `actionType`, `groupName?` | `actionType` 取值可为 `like` / `unlike` / `favorite` / `unfavorite` |
| 获取评论列表 | GET | `/comments` | `objectId`, `page`, `size` | 返回公开评论列表 |
| 提交评论 | POST | `/comments` | `userId`, `objectId`, `content`, `parentId?` | 新评论默认进入审核队列 |
| 上传照片 | POST | `/photos/upload` | `userId`, `objectId`, `file`, `location`, `description` | 上传成功后返回内容 ID |
| 查询审核状态 | GET | `/audit/status/{contentId}` | `contentId` | 查询评论或照片的审核结果 |

在当前实现中，上述接口以本地 `InteractionStore` 替代，页面层不直接依赖后端，实现了后续替换时的低耦合。

---

根据团长提供的实际接口信息（`http://60.205.14.101:8080/api/v1` 及示例 `/auth/current-user`），我修改了设计文档中的 **6.5 节（框架统筹与用户系统模块）**。你只需将原文档中的这一节整体替换为下面的内容即可。

---

### 6.5 框架统筹与用户系统模块（潘晨晨 编写）

#### 6.5.1 模块概述

本模块负责掌上博物馆 App 的全局框架搭建与用户个人信息管理，主要包括：

- **应用初始化与路由框架**：底部 Tab 导航、页面路由注册、全局状态管理。
- **网络层封装**：统一 HTTP 请求工具（`HttpApi`），支持 GET/POST 及后续 Token 自动附加。
- **用户认证**：登录、注册、登出，使用 `AppStorage` 管理全局登录状态。
- **个人主页**：展示用户信息、收藏/点赞/评论/上传入口，集成退出登录。
- **隐私设置**：用户可控制收藏、评论、上传照片的可见范围，数据存于本地 `Preferences`，为后续后端同步预留。

当前实现完全基于 **Mock 数据 + 本地存储**，所有用户交互（注册、登录、隐私设置）均不依赖后端真实接口，但已预留 `UserService` 和 `HttpApi` 统一替换点，后续只需修改 `USE_MOCK` 开关即可切换至真实 API。

#### 6.5.2 启动与初始化流程

```mermaid
flowchart TD
    Start[App 启动] --> Splash[Index 页（底部 Tab）]
    Splash --> Init[PreferencesUtil.init 初始化]
    Init --> CheckLogin{AppStorage 中<br>isLogin 是否为 true?}
    
    CheckLogin -- 是 --> Home[展示已登录界面]
    CheckLogin -- 否/未登录 --> Profile[个人中心显示未登录]
    Profile --> Login[用户点击“去登录”]
    Login --> Auth[LoginPage 登录流程]
```

- 应用入口为 `Index.ets`，包含底部 Tab（首页、发现、我的）。
- 登录状态通过 `AppStorage.SetOrCreate('isLogin', true/false)` 全局管理，所有页面可通过 `@StorageLink('isLogin')` 自动响应。
- 用户信息（用户名）同样存储在 `AppStorage` 中，个人中心可直接读取。

#### 6.5.3 用户注册与登录设计

##### 6.5.3.1 总体架构

用户认证相关逻辑全部封装在 `UserService` 中，页面仅负责 UI 交互和状态变更。当前使用 `USE_MOCK = true` 模拟登录注册，未来只需修改该常量为 `false` 并确保后端接口路径正确即可无缝切换。

```typescript
// common/services/UserService.ets（核心结构）
const USE_MOCK = true;
const BASE_URL = 'http://60.205.14.101:8080/api/v1'; // 实际服务器地址

export class UserService {
  static async login(username: string, password: string): Promise<LoginResult>
  static async register(username: string, password: string): Promise<RegisterResult>
  static async getUserInfo(userId: string): Promise<UserInfo>
}
```

##### 6.5.3.2 登录流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant LoginPage as LoginPage
    participant UserService as UserService
    participant AppStorage as AppStorage

    User->>LoginPage: 输入用户名/密码
    LoginPage->>LoginPage: 前端校验（非空、长度）
    LoginPage->>UserService: login(username, password)
    alt Mock 模式
        UserService->>UserService: 模拟延迟，校验任意非空
        UserService-->>LoginPage: { success: true, username }
    else 真实 API
        UserService->>HttpApi: POST /api/v1/auth/login
        HttpApi-->>UserService: { success, username, token? }
        UserService-->>LoginPage: 返回结果
    end
    LoginPage->>AppStorage: SetOrCreate('isLogin', true)
    LoginPage->>AppStorage: SetOrCreate('username', username)
    LoginPage->>User: 提示成功，返回上一页
```

**关键代码特征**：
- 登录成功后将 `isLogin` 和 `username` 存入 `AppStorage`，实现跨页面状态共享。
- `LoginPage` 中的模拟延迟 `setTimeout` 仅用于演示，实际已迁移至 `UserService` 内部控制。

##### 6.5.3.3 注册流程

与登录类似，`RegisterPage` 调用 `UserService.register`（真实 API 路径为 `POST /api/v1/auth/register`），成功后将用户标记为已登录并返回。

##### 6.5.3.4 Token 管理（预留）

真实 API 登录成功后应返回 JWT Token，前端使用 `Preferences` 存储，并在后续请求中通过 `HttpApi` 自动附加 `Authorization: Bearer {token}` 头。当前 `HttpApi` 已预留 `getToken()` 方法，待联调时启用。

#### 6.5.4 个人主页设计（ProfilePage）

##### 6.5.4.1 页面结构与状态

个人主页根据登录状态展示不同内容：

- **未登录**：显示默认头像、“未登录”文字，以及“去登录”“注册账号”按钮。
- **已登录**：显示用户头像（占位）、用户名，以及功能菜单（收藏夹、浏览记录、我的评论、上传的照片、退出登录）。

```typescript
// ProfilePage.ets 核心状态
@StorageLink('isLogin') isLogin: boolean = false;
@StorageLink('username') username: string = '';
```

##### 6.5.4.2 交互流程

```mermaid
flowchart TD
    A[ProfilePage 加载] --> B{检查 isLogin}
    B -- false --> C[显示未登录界面]
    C --> D[点击“去登录”]
    D --> E[router.pushUrl 至 LoginPage]
    E --> F[登录成功后更新 AppStorage]
    F --> A
    
    B -- true --> G[显示已登录界面]
    G --> H[点击“退出登录”]
    H --> I[调用 UserService.logout 或直接<br>AppStorage.Set('isLogin',false)]
    I --> A
```

- 个人主页的功能菜单（收藏夹、浏览记录等）当前仅弹出 Toast 提示“功能开发中”，后续由刘清同学集成具体页面。
- 退出登录时调用 `preferencesUtil.clearAll()` 清除本地存储（用户设置、评论等），并重置 `AppStorage` 中的登录状态。

#### 6.5.5 隐私设置设计（PrivacySettingPage）

##### 6.5.5.1 功能概述

用户可分别控制以下四项内容的可见性：
- 收藏夹可见（showFavorites）
- 点赞记录可见（showLikes）  
- 评论可见（showComments）
- 上传照片可见（showPhotos）

设置项通过 `Toggle` 开关实时修改，数据持久化到 `Preferences` 中，以用户名为键隔离不同用户。

##### 6.5.5.2 数据模型与存储

```typescript
// common/models/InteractionModels.ets
export interface PrivacySettings {
  showFavorites: boolean;
  showLikes: boolean;
  showComments: boolean;
  showPhotos: boolean;
}

export const DEFAULT_PRIVACY_SETTINGS: PrivacySettings = {
  showFavorites: true,
  showLikes: true, 
  showComments: true,
  showPhotos: true
};
```

存储服务 `interactionStore` 提供 `getPrivacy(username)` 和 `savePrivacy(username, settings)` 方法，内部使用 `Preferences` 读写。

##### 6.5.5.3 页面交互

```mermaid
sequenceDiagram
    participant User as 用户
    participant PrivacyPage as PrivacySettingPage
    participant Store as interactionStore
    participant Prefs as Preferences

    User->>PrivacyPage: 打开隐私设置页
    PrivacyPage->>Store: getPrivacy(username)
    Store->>Prefs: 读取缓存
    Prefs-->>Store: 返回设置
    Store-->>PrivacyPage: 填充 @State privacy
    PrivacyPage-->>User: 渲染三个开关

    User->>PrivacyPage: 切换某个开关
    PrivacyPage->>PrivacyPage: 更新 @State privacy
    PrivacyPage->>Store: savePrivacy(username, newSettings)
    Store->>Prefs: 写入
    Prefs-->>Store: 成功
    PrivacyPage->>User: Toast 提示“设置已更新”
```

- 所有设置仅存储本地，不依赖后端，但设计上已预留接口字段。后续联调时可通过 `UserService.updatePrivacy` 调用 `POST /api/v1/user/privacy` 同步到服务器。
- 他人个人信息页（`OtherProfilePage`）在展示用户公开内容时，需根据目标用户的隐私设置过滤数据（当前为模拟实现，未来需调用后端接口获取过滤后的列表）。

#### 6.5.6 网络层与公共工具

##### 6.5.6.1 HttpApi 封装

`common/network/HttpApi.ets` 提供统一的 GET/POST 方法，支持泛型返回类型，内置超时和异常处理。

```typescript
// 实际服务器地址（根据团长提供的文档）
const BASE_URL = 'http://60.205.14.101:8080/api/v1';

export class HttpApi {
  static async get<T>(url: string, params?: Record<string, string>): Promise<T>
  static async post<T>(url: string, body: object): Promise<T>
}
```

- 请求方法内部自动调用 `getToken()`，待 Token 就绪后可统一附加 `Authorization` 头。

##### 6.5.6.2 PreferencesUtil 本地存储

`common/utils/PreferencesUtil.ets` 封装了鸿蒙 `preferences` API，提供静态方法：

- `init(context)`: 初始化 Preferences 实例。
- `putString(key, value)`, `getString(key, def)`: 字符串读写。
- `clearAll()`: 清空当前用户的全部数据（退出登录时调用）。

在 `EntryAbility.onCreate` 中调用 `PreferencesUtil.init(this.context)` 完成全局初始化。

#### 6.5.7 与其它子系统的集成约定

根据团长提供的实际接口信息，当前后端服务地址为 `http://60.205.14.101:8080`，API 前缀为 `/api/v1`。以下为各功能依赖的接口路径（待后台管理子系统提供完整文档后确认）：

| 功能 | 依赖子系统 | 接口路径 | 当前状态 |
|------|-----------|----------|----------|
| 用户注册 | 后台管理子系统 | `POST /api/v1/auth/register` | 未对接，使用 Mock |
| 用户登录 | 后台管理子系统 | `POST /api/v1/auth/login` | 未对接，使用 Mock |
| 获取当前用户信息 | 后台管理子系统 | `GET /api/v1/auth/current-user` | 已有示例，待联调 |
| 更新隐私设置 | 后台管理子系统 | `POST /api/v1/user/privacy` | 未对接，仅本地存储 |
| 获取他人公开信息 | 后台管理子系统 | `GET /api/v1/user/{userId}/public` | 未对接，模拟数据 |

> **说明**：
> - 实际路径以后台管理子系统提供的正式接口文档为准，表中路径为基于团长示例的合理推测。
> - 所有接口均已预留调用位置，且通过 `USE_MOCK` 开关控制。当后端接口就绪后，只需将 `USE_MOCK` 改为 `false`，并确认 `HttpApi.BASE_URL` 正确，即可无缝切换。

---


## 7. 非功能性设计

### 7.1 性能设计

#### 7.1.1 图片加载优化
- **策略**：列表页使用缩略图（?size=thumb 参数），详情页加载原图
- **缓存**：使用 Image 组件内置缓存机制，避免重复下载
- **占位图**：加载中显示骨架屏或默认占位图

#### 7.1.2 语音响应优化
- **语音识别超时**：设置为 10 秒，超时后提示用户重试
- **语音合成预加载**：进入文物详情页时预加载语音讲解
- **SSE 流式展示**：语音问答采用流式响应，逐字显示答案，减少等待感

#### 7.1.3 列表性能优化
- 首页文物列表使用 LazyForEach 实现懒加载
- 分页加载，每页 20 条

### 7.2 安全设计

#### 7.2.1 认证与授权
- 采用 JWT 无状态认证机制
- Token 设置过期时间（建议 2 小时）
- 请求在 HttpUtil 中统一携带 `Authorization: Bearer {token}`

#### 7.2.2 数据安全
- 用户密码使用 bcrypt 加密存储（后端）
- 密码等敏感信息不在本地明文存储
- 所有 API 通信采用 HTTPS 加密

#### 7.2.3 权限管理
- 调用系统敏感能力（相机、麦克风）前动态申请权限
- 用户可在设置中随时撤销权限
- 未授权时给出明确提示并引导用户授权

#### 7.2.4 输入校验
- 所有用户输入进行前端校验（长度、格式等）
- 防止 XSS 攻击，对用户生成内容进行转义处理

### 7.3 容错与异常处理

#### 7.3.1 网络异常处理

| 场景 | 处理策略 |
|---|---|
| 无网络连接 | 显示“网络不可用”提示，展示本地缓存数据 |
| 请求超时 | 自动重试 1 次，仍失败则提示用户稍后再试 |
| 服务器错误（5xx） | 提示“服务器繁忙，请稍后再试” |
| Token 过期 | 自动跳转登录页，提示重新登录 |

#### 7.3.2 功能降级
- **语音识别失败**：降级为手动输入搜索关键词
- **图片搜索超时**：提示用户“检索超时，请尝试重新上传”
- **音视频加载失败**：显示“加载失败，点击重试”按钮

### 7.4 可扩展性设计
- 模块间通过接口解耦，新增功能模块不影响已有代码
- 网络层封装支持后续更换后端地址或添加拦截器
- 数据模型独立定义，便于与后端协商调整字段



## 8. 附录

### 8.1 与其它子系统的集成约定
[预留接口文档链接或说明，待与其他子系统组长协商后补充]

### 8.2 Git 协作规范

1. **禁止直接推送到 `main` 分支**
2. 每位组员基于 `main` 创建自己的功能分支，命名格式：`feature/<模块名>`（如 `feature/browse-module`）
3. 每天工作结束前将分支推送至远端备份
4. 合并时发起 Pull Request，由组长审核后合并
5. 合并冲突由开发者本地解决后重新推送

### 8.3 GitHub 仓库信息
- 仓库地址：`https://github.com/BUCT-CS2301/PalmMuseum.git`
- 主分支：`main`

### 8.4 设计变更记录

| 日期 | 变更内容 | 变更原因 | 影响范围 | 记录人 |
|---|---|---|---|---|
|  |  |  |  |  |

### 8.5 用户交互模块设计补充（2026-05-31）

为与当前代码实现保持一致，用户交互模块设计补充如下。

#### 8.5.1 当前实现状态

当前前端并非仅实现“点赞、收藏、评论、上传照片”四项基础能力，还已经扩展到：

- 评论回复树
- 评论点赞
- 收藏夹自定义新增、删除
- 收藏内容移动分组
- 浏览记录自动记录
- 个人主页统计联动
- 隐私设置读写

实现核心仍基于本地 `Preferences` 与 `AppStorage`，由 `InteractionStore` 统一封装，页面层不直接操作底层存储。

#### 8.5.2 审核流设计修正

原型阶段审核流采用“前端本地模拟”方案：

1. 评论或照片提交后先写入本地存储，状态为 `pending`
2. 页面通过定时刷新或重新读取数据触发状态同步
3. 本地逻辑在约 `1.5s` 后自动将记录转为 `approved`

该设计用于模拟真实审核过程，后续接入后台管理子系统时，仅需将本地同步逻辑替换为服务端审核状态查询逻辑。

#### 8.5.3 数据模型补充

建议用户交互模块联调时，以以下字段作为最小公共模型：

- `CommentItem`
  - `id`
  - `artifactId`
  - `artifactTitle`
  - `username`
  - `content`
  - `createdAt`
  - `status`
  - `parentId`
  - `replyTo`
  - `likeCount`
  - `likedByCurrentUser`
- `FavoriteItem`
  - `artifactId`
  - `title`
  - `imageUrl`
  - `period`
  - `museum`
  - `groupName`
  - `createdAt`
- `UploadedPhotoItem`
  - `id`
  - `artifactId`
  - `artifactTitle`
  - `username`
  - `imageUri`
  - `location`
  - `description`
  - `createdAt`
  - `status`
- `UserInteractionStats`
  - `likes`
  - `favorites`
  - `comments`
  - `photos`
  - `history`

#### 8.5.4 接口设计修正建议

原设计中将点赞/收藏笼统合并为 `/user/action`，对于当前实现已不够细。建议后续按资源拆分：

- 文物点赞
  - `GET /artifacts/{objectId}/interaction-summary`
  - `POST /artifacts/{objectId}/likes`
  - `DELETE /artifacts/{objectId}/likes`
- 收藏与收藏夹
  - `GET /users/{userId}/favorite-groups`
  - `POST /users/{userId}/favorite-groups`
  - `DELETE /users/{userId}/favorite-groups/{groupName}`
  - `GET /users/{userId}/favorites`
  - `POST /users/{userId}/favorites`
  - `DELETE /users/{userId}/favorites/{objectId}`
  - `PATCH /users/{userId}/favorites/{objectId}`
- 评论
  - `GET /artifacts/{objectId}/comments`
  - `POST /artifacts/{objectId}/comments`
  - `POST /comments/{commentId}/likes`
  - `GET /users/{userId}/comments`
- 照片
  - `POST /artifacts/{objectId}/photos`
  - `GET /users/{userId}/photos`
  - `GET /audit/status/{contentId}`
- 统计与隐私
  - `POST /users/{userId}/history`
  - `GET /users/{userId}/history`
  - `GET /users/{userId}/interaction-stats`
  - `GET /users/{userId}/privacy`
  - `PUT /users/{userId}/privacy`

#### 8.5.5 文档与代码对齐说明

后续如继续维护本模块文档，建议统一以以下文档为接口主参照：

- [Doc/用户交互文档检查与接口整理.md](F:\PalmMuseum\Doc\用户交互文档检查与接口整理.md)

该文档已经整理了：

- 当前文档缺口
- 当前代码对应的数据模型
- 接口字段建议
- 最小联调接口集合